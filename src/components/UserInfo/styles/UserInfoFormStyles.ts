import { styled } from '@mui/material';

import { ListItem } from '@mui/material';

const UserInfoListItem = styled(ListItem)(() => ({
	display: 'flex',
	flexDirection: 'column',
}));

export { UserInfoListItem };
